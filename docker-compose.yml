services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sanggabuana_app
    restart: always
    ports:
      - "80:80"
    volumes:
      - sanggabuana_storage:/var/www/html/storage
      - ./.env:/var/www/html/.env
    depends_on:
      - db
    env_file:
      - .env

  db:
    image: mysql:8.0
    container_name: sanggabuana_db
    restart: always
    environment:
      MYSQL_DATABASE: ${DB_DATABASE:-sanggabuana}
      MYSQL_USER: ${DB_USERNAME:-sanggabuana}
      MYSQL_PASSWORD: ${DB_PASSWORD:-secret}
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-root_secret}
    volumes:
      - sanggabuana_db_data:/var/lib/mysql
    ports:
      - "3306:3306"

volumes:
  sanggabuana_storage:
  sanggabuana_db_data:
